# A function is some stored code that we use. A function takes some input
# and produces an output. The output is called the "return" of the function
# float (): converts a string or an integer into float value
# int (): converts a string into integer value
# a function () blows up if wrong input is given to it

string = raw_input("Enter string: ")
# to print the letter with the maximum value
big = max(string)
print "letter with the maximum value: ",big

